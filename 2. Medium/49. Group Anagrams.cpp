#include <iostream>
#include <bits/stdc++.h>

using namespace std;

int main() {
    vector<string> list = {"eat", "tea", "tan", "ate", "nat", "bat"};
    vector<vector<string>> result;
    char m1, m2;
    map<char, int> l, r;

    for(int i = 0; i<list[i].size(); i++){
        m1 = list[0][i];
        l[m1]++;
    }

    for(int i = 1; i<list.size(); i++){
        while(int j = 0 < list.size()){
            
        }
    }

    return 0;
}
